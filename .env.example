# Copie para .env e ajuste.

# LLM em modo fake (offline, sem custo) — default do stack local.
# Para usar a API real da Anthropic: LLM_USE_FAKE=false + ANTHROPIC_API_KEY.
LLM_USE_FAKE=true
LLM_FAKE_SCENARIO=react-tool-call
ANTHROPIC_API_KEY=
